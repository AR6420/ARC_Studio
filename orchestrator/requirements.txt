# A.R.C Studio — Orchestrator dependencies
# Python 3.11+

# ── Web framework ─────────────────────────────────────────────────────────────
fastapi>=0.115.0
uvicorn[standard]>=0.30.0
# python-multipart: required by FastAPI for any route that takes Form() or
# UploadFile parameters. Missing-dep error surfaces only at first request to
# such a route, not at import time — caught on the Phase 3 droplet smoke run.
python-multipart>=0.0.9

# ── LLM clients ───────────────────────────────────────────────────────────────
# anthropic: production path (LLM_PROVIDER=anthropic, default)
# openai:    vLLM / OpenAI-compatible path (LLM_PROVIDER=vllm), used on
#            competition/amd-hackathon for MI300X-served Qwen models
anthropic>=0.36.0
openai>=1.40.0

# ── Async HTTP client (for TRIBE v2 and MiroFish calls) ───────────────────────
httpx>=0.27.0

# ── Configuration and validation ──────────────────────────────────────────────
pydantic>=2.8.0
pydantic-settings>=2.4.0

# ── Database ──────────────────────────────────────────────────────────────────
aiosqlite>=0.20.0

# ── Environment variable loading ──────────────────────────────────────────────
python-dotenv>=1.0.0

# ── Server-Sent Events (real-time campaign progress streaming) ────────────────
sse-starlette>=2.1.0

# FastAPI service dependencies for TRIBE v2 scorer
# Install TRIBE v2 itself from the vendored directory:
#   pip install -e ./vendor/tribev2

# Web framework
fastapi>=0.111.0,<0.120.0
uvicorn[standard]>=0.29.0,<0.35.0
pydantic>=2.7.0,<3.0.0
pydantic-settings>=2.2.0,<3.0.0

# Scientific / ML stack (must be compatible with tribev2 requirements)
numpy>=2.0.0,<3.0.0
# PyTorch >=2.5.1: needed for tribev2 + transformers compatibility.
# Upper ceiling relaxed (was <2.7 as a sm_120-workaround pin) — local preflight
# confirms torch 2.8+/cu128 has native sm_120 support; ROCm 6.2+ wheels exist
# for the same range. Hackathon target (MI300X / ROCm) and dev box (sm_120)
# both want the freedom to install >=2.7.
torch>=2.5.1,<3.0

# Required by tribev2 directly (listed here for explicit visibility)
# transformers>=4.45.0 needed for Whisper-large-v3 with return_timestamps="word"
# (replaces whisperx + ctranslate2 + faster-whisper to keep the dep tree pure
# PyTorch on ROCm — see scoring/whisper_hf.py).
transformers>=4.45.0
torchaudio>=2.5.0,<3.0
huggingface_hub>=0.23.0
einops>=0.7.0
gtts>=2.5.0
langdetect>=1.0.9
soundfile>=0.12.0
spacy>=3.7.0
Levenshtein>=0.25.0
julius>=0.2.7

# Async worker support
anyio>=4.3.0

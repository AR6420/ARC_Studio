---
phase: 05-orchestrator-integration-pipeline
plan: 07
type: execute
wave: 4
depends_on: ["05-05", "05-06"]
files_modified:
  - orchestrator/cli.py
  - orchestrator/__main__.py
  - orchestrator/tests/test_cli.py
autonomous: false
requirements:
  - ORCH-14

must_haves:
  truths:
    - "CLI command creates a campaign, runs single-iteration pipeline, and prints results summary"
    - "CLI prints variant IDs, composite scores, system availability warnings, and Opus analysis ranking"
    - "CLI uses CampaignRunner programmatically without needing the FastAPI server running"
    - "End-to-end flow produces: N variants, neural scores per variant, simulation metrics per variant, 7 composite scores, and cross-system analysis"
  artifacts:
    - path: "orchestrator/cli.py"
      provides: "CLI entry point for running campaigns from command line"
      exports: ["main", "run_campaign"]
  key_links:
    - from: "orchestrator/cli.py"
      to: "orchestrator/engine/campaign_runner.py"
      via: "CampaignRunner.run_single_iteration() called directly"
      pattern: "run_single_iteration"
    - from: "orchestrator/cli.py"
      to: "orchestrator/storage/database.py"
      via: "Database initialized directly for CLI use"
      pattern: "Database"
    - from: "orchestrator/cli.py"
      to: "orchestrator/storage/campaign_store.py"
      via: "CampaignStore for campaign creation"
      pattern: "CampaignStore"
---

<objective>
Build the CLI entry point that runs a full campaign from the command line without needing the FastAPI server, then verify the complete end-to-end pipeline with a human checkpoint.

Purpose: ORCH-14 requires end-to-end CLI execution producing variants, scores, metrics, and analysis. This is the final integration proving all components wire together correctly.

Output: orchestrator/cli.py with smoke test, followed by human verification of the full pipeline.
</objective>

<execution_context>
@C:/Users/adars/Downloads/ARC_Studio/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/adars/Downloads/ARC_Studio/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/05-orchestrator-integration-pipeline/05-CONTEXT.md
@.planning/phases/05-orchestrator-integration-pipeline/05-RESEARCH.md

@.planning/phases/05-orchestrator-integration-pipeline/05-01-SUMMARY.md
@.planning/phases/05-orchestrator-integration-pipeline/05-05-SUMMARY.md
@.planning/phases/05-orchestrator-integration-pipeline/05-06-SUMMARY.md

<interfaces>
<!-- From Plan 05 (campaign runner) -->
From orchestrator/engine/campaign_runner.py:
```python
class CampaignRunner:
    def __init__(self, variant_generator, tribe_scoring, mirofish_runner, result_analyzer, campaign_store, tribe_client, mirofish_client): ...
    async def run_single_iteration(self, campaign_id, iteration_number=1, previous_iteration_results=None, previous_analysis=None) -> dict[str, Any]: ...
```

<!-- From Plan 01 (storage) -->
From orchestrator/storage/database.py:
```python
class Database:
    def __init__(self, path: str): ...
    async def connect(self) -> None: ...
    async def close(self) -> None: ...
```

From orchestrator/storage/campaign_store.py:
```python
class CampaignStore:
    def __init__(self, db: Database): ...
    async def create_campaign(self, request: CampaignCreateRequest) -> CampaignResponse: ...
```

From orchestrator/api/schemas.py:
```python
class CampaignCreateRequest(BaseModel):
    seed_content: str; prediction_question: str; demographic: str; ...
```

<!-- From Plan 02 (clients) -->
From orchestrator/clients/tribe_client.py:
```python
class TribeClient:
    def __init__(self, client: httpx.AsyncClient): ...
```

From orchestrator/clients/mirofish_client.py:
```python
class MirofishClient:
    def __init__(self, client: httpx.AsyncClient): ...
```

From orchestrator/clients/claude_client.py:
```python
class ClaudeClient:
    def __init__(self) -> None: ...
```

<!-- From Plan 03 (engine) -->
From orchestrator/engine/variant_generator.py:
```python
class VariantGenerator:
    def __init__(self, claude_client: ClaudeClient): ...
```

From orchestrator/engine/tribe_scorer.py:
```python
class TribeScoringPipeline:
    def __init__(self, tribe_client: TribeClient): ...
```

From orchestrator/engine/mirofish_runner.py:
```python
class MirofishRunner:
    def __init__(self, mirofish_client: MirofishClient): ...
```

From orchestrator/engine/result_analyzer.py:
```python
class ResultAnalyzer:
    def __init__(self, claude_client: ClaudeClient): ...
```

From orchestrator/config.py:
```python
settings.tribe_scorer_url   # http://localhost:8001
settings.mirofish_url       # http://localhost:5000
settings.database_path_absolute  # resolved Path
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create CLI entry point for end-to-end campaign execution</name>
  <files>orchestrator/cli.py, orchestrator/__main__.py, orchestrator/tests/test_cli.py</files>
  <read_first>orchestrator/engine/campaign_runner.py, orchestrator/storage/database.py, orchestrator/storage/campaign_store.py, orchestrator/config.py, orchestrator/api/schemas.py, orchestrator/clients/tribe_client.py, orchestrator/clients/mirofish_client.py, orchestrator/clients/claude_client.py, orchestrator/engine/variant_generator.py, orchestrator/engine/tribe_scorer.py, orchestrator/engine/mirofish_runner.py, orchestrator/engine/result_analyzer.py</read_first>
  <action>
Create `orchestrator/cli.py`:

```python
"""
CLI entry point for running Nexus Sim campaigns from the command line.

Usage:
    python -m orchestrator.cli --seed-content "Your content here..." --prediction-question "How will the audience react?" --demographic tech_professionals

    # Or with a file:
    python -m orchestrator.cli --seed-file content.txt --prediction-question "..." --demographic tech_professionals

Per ORCH-14: Produces variants, scores, metrics, and analysis without needing the FastAPI server.
"""

import argparse
import asyncio
import json
import logging
import sys
from pathlib import Path

import httpx

from orchestrator.config import settings
from orchestrator.storage.database import Database
from orchestrator.storage.campaign_store import CampaignStore
from orchestrator.clients.claude_client import ClaudeClient
from orchestrator.clients.tribe_client import TribeClient
from orchestrator.clients.mirofish_client import MirofishClient
from orchestrator.engine.variant_generator import VariantGenerator
from orchestrator.engine.tribe_scorer import TribeScoringPipeline
from orchestrator.engine.mirofish_runner import MirofishRunner
from orchestrator.engine.result_analyzer import ResultAnalyzer
from orchestrator.engine.campaign_runner import CampaignRunner
from orchestrator.api.schemas import CampaignCreateRequest

logger = logging.getLogger(__name__)


def parse_args(argv: list[str] | None = None) -> argparse.Namespace:
    parser = argparse.ArgumentParser(
        description="Run a Nexus Sim campaign from the command line",
        prog="python -m orchestrator.cli",
    )
    content_group = parser.add_mutually_exclusive_group(required=True)
    content_group.add_argument("--seed-content", type=str, help="Seed content text (inline)")
    content_group.add_argument("--seed-file", type=str, help="Path to a text file with seed content")

    parser.add_argument("--prediction-question", required=True, type=str,
                       help="What you want to know about audience response")
    parser.add_argument("--demographic", required=True, type=str,
                       help="Demographic preset key (e.g., tech_professionals) or 'custom'")
    parser.add_argument("--demographic-custom", type=str, default=None,
                       help="Custom demographic description (when --demographic=custom)")
    parser.add_argument("--agent-count", type=int, default=40, help="Number of MiroFish agents (default: 40)")
    parser.add_argument("--constraints", type=str, default=None, help="Brand guidelines or constraints")
    parser.add_argument("--output", type=str, default=None, help="Path to write JSON results (default: stdout)")
    parser.add_argument("--verbose", "-v", action="store_true", help="Enable verbose logging")

    return parser.parse_args(argv)


async def run_campaign(args: argparse.Namespace) -> dict:
    """
    Run a single-iteration campaign programmatically.
    Initializes all dependencies, creates campaign, runs pipeline, returns results.
    """
    # Initialize all resources
    db = Database(str(settings.database_path_absolute))
    await db.connect()

    tribe_http = httpx.AsyncClient(base_url=settings.tribe_scorer_url, timeout=120.0)
    mirofish_http = httpx.AsyncClient(base_url=settings.mirofish_url, timeout=300.0)

    try:
        # Create all components
        store = CampaignStore(db)
        claude = ClaudeClient()
        tribe_client = TribeClient(tribe_http)
        mirofish_client = MirofishClient(mirofish_http)

        variant_gen = VariantGenerator(claude)
        tribe_scoring = TribeScoringPipeline(tribe_client)
        mirofish_runner = MirofishRunner(mirofish_client)
        result_analyzer = ResultAnalyzer(claude)

        runner = CampaignRunner(
            variant_generator=variant_gen,
            tribe_scoring=tribe_scoring,
            mirofish_runner=mirofish_runner,
            result_analyzer=result_analyzer,
            campaign_store=store,
            tribe_client=tribe_client,
            mirofish_client=mirofish_client,
        )

        # Load seed content
        if args.seed_file:
            seed_content = Path(args.seed_file).read_text(encoding="utf-8")
        else:
            seed_content = args.seed_content

        # Create campaign
        request = CampaignCreateRequest(
            seed_content=seed_content,
            prediction_question=args.prediction_question,
            demographic=args.demographic,
            demographic_custom=args.demographic_custom,
            agent_count=args.agent_count,
            constraints=args.constraints,
            auto_start=False,
        )
        campaign = await store.create_campaign(request)
        print(f"\n{'='*60}")
        print(f"Campaign created: {campaign.id}")
        print(f"Demographic: {args.demographic}")
        print(f"Agent count: {args.agent_count}")
        print(f"{'='*60}\n")

        # Run single iteration
        result = await runner.run_single_iteration(
            campaign_id=campaign.id,
            iteration_number=1,
        )

        # Print summary
        _print_summary(result)

        return result

    finally:
        await tribe_http.aclose()
        await mirofish_http.aclose()
        await db.close()


def _print_summary(result: dict) -> None:
    """Print a human-readable summary of campaign results."""
    print(f"\n{'='*60}")
    print("CAMPAIGN RESULTS")
    print(f"{'='*60}\n")

    # System availability
    avail = result.get("system_availability", {})
    warnings = result.get("warnings", [])
    print(f"TRIBE v2: {'Available' if avail.get('tribe_available') else 'UNAVAILABLE'}")
    print(f"MiroFish: {'Available' if avail.get('mirofish_available') else 'UNAVAILABLE'}")
    if warnings:
        for w in warnings:
            print(f"  WARNING: {w}")
    print()

    # Variants and scores
    variants = result.get("variants", [])
    tribe_scores = result.get("tribe_scores", [])
    mirofish_metrics = result.get("mirofish_metrics", [])
    composite_scores = result.get("composite_scores", [])

    for i, variant in enumerate(variants):
        print(f"--- Variant: {variant.get('id', f'v{i+1}')} ---")
        print(f"Strategy: {variant.get('strategy', 'N/A')}")
        print(f"Content preview: {variant.get('content', '')[:150]}...")
        print()

        # TRIBE scores
        tribe = tribe_scores[i] if i < len(tribe_scores) and tribe_scores[i] else None
        if tribe:
            print("  TRIBE v2 scores:")
            for dim, score in tribe.items():
                print(f"    {dim}: {score:.1f}")
        else:
            print("  TRIBE v2 scores: N/A")

        # MiroFish metrics
        miro = mirofish_metrics[i] if i < len(mirofish_metrics) and mirofish_metrics[i] else None
        if miro:
            print("  MiroFish metrics:")
            for key, val in miro.items():
                if not isinstance(val, list):
                    print(f"    {key}: {val}")
        else:
            print("  MiroFish metrics: N/A")

        # Composite scores
        comp = composite_scores[i] if i < len(composite_scores) else {}
        if comp:
            print("  Composite scores:")
            for name, score in comp.items():
                print(f"    {name}: {score if score is not None else 'N/A'}")
        print()

    # Analysis
    analysis = result.get("analysis", {})
    ranking = analysis.get("ranking", [])
    if ranking:
        print(f"RANKING: {' > '.join(ranking)}")
    print()

    insights = analysis.get("cross_system_insights", [])
    if insights:
        print("CROSS-SYSTEM INSIGHTS:")
        for insight in insights:
            print(f"  - {insight}")
    print()

    recs = analysis.get("recommendations_for_next_iteration", [])
    if recs:
        print("RECOMMENDATIONS:")
        for rec in recs:
            print(f"  - {rec}")

    print(f"\n{'='*60}\n")


def main(argv: list[str] | None = None):
    args = parse_args(argv)

    if args.verbose:
        logging.basicConfig(level=logging.DEBUG, format="%(asctime)s [%(name)s] %(levelname)s: %(message)s")
    else:
        logging.basicConfig(level=logging.INFO, format="%(asctime)s %(levelname)s: %(message)s")

    result = asyncio.run(run_campaign(args))

    if args.output:
        # Write full results as JSON
        output_path = Path(args.output)
        # Convert to JSON-serializable format (handle None, etc.)
        with output_path.open("w", encoding="utf-8") as f:
            json.dump(result, f, indent=2, default=str)
        print(f"Full results written to {output_path}")


if __name__ == "__main__":
    main()
```

Also create `orchestrator/__main__.py`:
```python
from orchestrator.cli import main
main()
```

Create `orchestrator/tests/test_cli.py`:

Smoke test with ALL external dependencies mocked (Claude, TRIBE, MiroFish):

1. `test_parse_args_basic`: verify parse_args works with required flags
2. `test_parse_args_seed_file`: verify --seed-file flag is parsed
3. `test_parse_args_missing_required`: verify argparse raises SystemExit when required args missing
4. `test_run_campaign_mocked`: mock Database, CampaignStore, ClaudeClient, TribeClient, MirofishClient to return canned data. Verify run_campaign completes and returns a dict with expected keys (variants, tribe_scores, mirofish_metrics, composite_scores, analysis).

The mocked test should verify the full wiring without hitting real services.
  </action>
  <verify>
    <automated>cd "C:/Users/adars/Downloads/ARC_Studio" && python -m pytest orchestrator/tests/test_cli.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - orchestrator/cli.py contains `def parse_args(`
    - orchestrator/cli.py contains `async def run_campaign(`
    - orchestrator/cli.py contains `def main(`
    - orchestrator/cli.py contains `CampaignRunner(`
    - orchestrator/cli.py contains `run_single_iteration`
    - orchestrator/cli.py contains `_print_summary`
    - orchestrator/cli.py contains `--seed-content`
    - orchestrator/cli.py contains `--prediction-question`
    - orchestrator/cli.py contains `--demographic`
    - orchestrator/__main__.py exists
    - orchestrator/tests/test_cli.py exits 0 with all tests passing
  </acceptance_criteria>
  <done>CLI creates campaign, runs full pipeline, and prints results summary with variant scores, composite scores, and Opus analysis; smoke tests pass</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verify full Phase 5 test suite and integration</name>
  <files>orchestrator/tests/</files>
  <action>
This is a human verification checkpoint. The executor should run all the verification commands below and present the results to the user for approval. No code changes needed in this task.

What was built: Complete Phase 5 orchestrator integration pipeline covering all 14 ORCH requirements:
- Pydantic schemas (ORCH-03)
- SQLite storage with campaign/iteration/analysis tables (ORCH-02)
- HTTP clients for TRIBE v2 and MiroFish (ORCH-07)
- Variant generator using Claude Haiku (ORCH-08)
- TRIBE scoring pipeline with sequential processing (ORCH-09)
- MiroFish simulation runner with 8-metric computation (ORCH-10)
- Composite scorer with all 7 formulas (ORCH-11)
- Result analyzer using Claude Opus (ORCH-12)
- Campaign runner wiring all components (ORCH-13)
- FastAPI app with CORS, lifespan, campaign CRUD (ORCH-01, ORCH-04)
- Health endpoint pinging downstream services (ORCH-05)
- Demographics endpoint returning 6 presets (ORCH-06)
- CLI entry point for end-to-end execution (ORCH-14)

Verification commands:
1. Run full test suite: `python -m pytest orchestrator/tests/ -v`
2. Verify app import: `python -c "from orchestrator.api import app; print('App:', app.title)"`
3. Verify CLI help: `python -m orchestrator.cli --help`
4. Verify engine imports: `python -c "from orchestrator.engine.campaign_runner import CampaignRunner; from orchestrator.engine.composite_scorer import compute_composite_scores; print('OK')"`
  </action>
  <verify>
    <automated>cd "C:/Users/adars/Downloads/ARC_Studio" && python -m pytest orchestrator/tests/ -v --tb=short</automated>
  </verify>
  <done>All tests pass, all modules import cleanly, CLI help displays correctly, user approves the complete Phase 5 implementation</done>
</task>

</tasks>

<verification>
```bash
cd "C:/Users/adars/Downloads/ARC_Studio" && python -m pytest orchestrator/tests/ -v --tb=short
```
Full test suite passes.
</verification>

<success_criteria>
- CLI runs campaign end-to-end from command line (per ORCH-14)
- CLI prints variant IDs, scores, composite scores, ranking, and cross-system insights
- CLI works without FastAPI server (direct component instantiation)
- Full test suite passes (all plans' tests combined)
- All module imports work cleanly
</success_criteria>

<output>
After completion, create `.planning/phases/05-orchestrator-integration-pipeline/05-07-SUMMARY.md`
</output>

---
phase: 09-validation-and-documentation
plan: 03
type: execute
wave: 2
depends_on:
  - 09-01
files_modified:
  - results/validation_report.md
autonomous: false
requirements:
  - VAL-02
  - VAL-03
  - VAL-04
  - VAL-05

must_haves:
  truths:
    - "All 5 demo scenarios have been executed end-to-end with results recorded as JSON"
    - "Iteration improvement occurs in at least 4 of 5 scenarios"
    - "Cross-system reasoning referencing both TRIBE and MiroFish appears in all 5 reports"
    - "Different demographics produce meaningfully different score profiles"
  artifacts:
    - path: "results/product_launch_result.json"
      provides: "Product launch scenario results"
      contains: "iterations"
    - path: "results/public_health_psa_result.json"
      provides: "PSA scenario results"
      contains: "iterations"
    - path: "results/price_increase_result.json"
      provides: "Price increase scenario results"
      contains: "iterations"
    - path: "results/policy_announcement_result.json"
      provides: "Policy announcement scenario results"
      contains: "iterations"
    - path: "results/gen_z_marketing_result.json"
      provides: "Gen Z marketing scenario results"
      contains: "iterations"
    - path: "results/validation_report.md"
      provides: "Written validation summary"
      contains: "VALIDATION"
  key_links:
    - from: "scripts/run_validation.py"
      to: "results/*.json"
      via: "writes result JSON files from CLI output"
      pattern: "result\\.json"
    - from: "scripts/validate_results.py"
      to: "results/*.json"
      via: "reads results and checks VAL-03, VAL-04, VAL-05 criteria"
      pattern: "json\\.load"
---

<objective>
Execute all 5 demo scenarios end-to-end, record their results, run the validation checker, and produce a final validation report summarizing whether the core hypothesis holds.

Purpose: This is the moment of truth -- VAL-02 (all scenarios run), VAL-03 (iteration improvement in 4+/5), VAL-04 (cross-system reasoning in all reports), and VAL-05 (demographic sensitivity). The user must have all services running for this plan.
Output: 5 result JSON files in results/, validation_report.md summarizing pass/fail
</objective>

<execution_context>
@C:/Users/adars/Downloads/ARC_Studio/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/adars/Downloads/ARC_Studio/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-validation-and-documentation/09-01-SUMMARY.md

@scripts/run_validation.py (automated runner from Plan 01)
@scripts/validate_results.py (results checker from Plan 01)
@scenarios/ (5 JSON test briefs from Plan 01)
@orchestrator/cli.py (CLI entry point)
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Verify all services are running before validation</name>
  <files>none</files>
  <action>
This is a human verification checkpoint. Before running the full validation suite, all backend services must be online. The executor should attempt to curl the health endpoints and present the results. The user confirms all services are ready.

Services to check:
- Orchestrator: curl http://localhost:8000/api/health
- TRIBE v2: curl http://localhost:8001/api/health
- MiroFish: curl http://localhost:5000/
- Docker: docker compose ps
- Dry-run: python scripts/run_validation.py --dry-run
  </action>
  <what-built>Plans 01 and 02 created the test briefs, run script, validation checker, and documentation. Before running the full validation suite, all backend services must be online.</what-built>
  <how-to-verify>
    1. Open a terminal and run: `curl http://localhost:8000/api/health`
       - Expected: JSON response showing service status for orchestrator, TRIBE v2, and MiroFish
    2. Verify TRIBE v2 is responding: `curl http://localhost:8001/api/health`
       - Expected: 200 OK response
    3. Verify MiroFish is responding: `curl http://localhost:5000/`
       - Expected: 200 OK response
    4. Verify Docker services: `docker compose ps`
       - Expected: Neo4j, Ollama, and LiteLLM containers running
    5. Run a quick dry-run to verify the script sees all scenarios:
       `python scripts/run_validation.py --dry-run`
       - Expected: 5 CLI commands printed, one per scenario

    If any service is not running, start it before proceeding:
    - `docker compose up -d` for infrastructure
    - `cd tribe_scorer && uvicorn main:app --port 8001` for TRIBE v2
    - Start MiroFish backend per its documentation
    - `cd orchestrator && uvicorn main:app --port 8000` for orchestrator
  </how-to-verify>
  <verify>User confirms all services are healthy and dry-run shows 5 scenarios</verify>
  <done>All services confirmed running by user</done>
  <resume-signal>Type "services ready" when all services are confirmed running, or describe any issues</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Run all 5 scenarios and validate results</name>
  <files>
    results/product_launch_result.json,
    results/public_health_psa_result.json,
    results/price_increase_result.json,
    results/policy_announcement_result.json,
    results/gen_z_marketing_result.json,
    results/validation_report.md
  </files>
  <read_first>
    scripts/run_validation.py (to understand run command),
    scripts/validate_results.py (to understand validation checks),
    scenarios/product_launch.json (to confirm brief format)
  </read_first>
  <action>
Execute the full validation suite. This task has significant runtime (estimated 15-60 minutes for 5 scenarios with 3 iterations each, depending on service response times).

**Step 1: Run all 5 scenarios**
Execute: `python scripts/run_validation.py`

This will:
- Run each of the 5 scenarios sequentially via the orchestrator CLI
- Write result JSON files to results/ directory
- Print progress as each scenario runs

If any scenario fails, note the failure but continue with remaining scenarios. A scenario failure does not block the others.

**Step 2: Validate results**
Execute: `python scripts/validate_results.py`

This will check:
- VAL-03: Iteration improvement in >= 4/5 scenarios (per D-03)
- VAL-04: Cross-system reasoning in all reports
- VAL-05: Demographic sensitivity across scenarios

**Step 3: Create validation report**
Based on the validate_results.py output, create `results/validation_report.md` with:

```markdown
# Nexus Sim Validation Report

**Date:** {today's date}
**Scenarios Run:** X/5
**Overall Status:** PASS/FAIL

## Summary

| Criterion | Requirement | Result | Status |
|-----------|-------------|--------|--------|
| Iteration Improvement (VAL-03) | >= 4/5 scenarios | X/5 | PASS/FAIL |
| Cross-System Reasoning (VAL-04) | 5/5 reports | X/5 | PASS/FAIL |
| Demographic Sensitivity (VAL-05) | Score variance > 5.0 | X.X | PASS/FAIL |

## Per-Scenario Details

### Scenario 1: Product Launch
- **Status:** completed/failed
- **Iterations completed:** X
- **Iteration improvement:** Yes/No (score trajectory: iter1=X, iter2=Y, iter3=Z)
- **Cross-system reasoning:** Found/Not found
- **Notable findings:** {brief observations}

[Repeat for each scenario]

## Hypothesis Validation

[Brief paragraph: Does the iterative feedback loop produce measurably better content?
Reference specific evidence from the results.]
```

If fewer than 5 scenarios complete, document which failed and why. Partial results are still valuable.
  </action>
  <verify>
    <automated>python -c "import os; result_files = [f for f in os.listdir('results') if f.endswith('_result.json')]; report = os.path.exists('results/validation_report.md'); print(f'Results: {len(result_files)}/5, Report: {report}')"</automated>
  </verify>
  <acceptance_criteria>
    - At least 5 result JSON files exist in results/ (one per scenario)
    - results/validation_report.md exists with summary table and per-scenario details
    - Validation report indicates PASS or FAIL for each of VAL-03, VAL-04, VAL-05
  </acceptance_criteria>
  <done>All 5 scenarios executed, results recorded, validation report written with pass/fail assessment for each criterion</done>
</task>

</tasks>

<verification>
- 5 result JSON files exist in results/
- validation_report.md summarizes pass/fail for VAL-03, VAL-04, VAL-05
- Iteration improvement demonstrated in at least 4/5 scenarios
</verification>

<success_criteria>
- All 5 demo scenarios ran end-to-end (VAL-02)
- Iteration improvement in >= 4/5 scenarios (VAL-03)
- Cross-system reasoning in all reports (VAL-04)
- Demographic changes meaningfully affect outcomes (VAL-05)
- Validation report documents all findings
</success_criteria>

<output>
After completion, create `.planning/phases/09-validation-and-documentation/09-03-SUMMARY.md`
</output>

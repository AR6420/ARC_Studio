---
phase: 08-ui-dashboard
plan: 08
type: execute
wave: 1
depends_on: []
files_modified: [ui/src/pages/campaign-detail.tsx]
autonomous: true
gap_closure: true
requirements: [UI-05, UI-06, UI-07, UI-08, UI-10]

must_haves:
  truths:
    - "Campaign tab displays 7 composite score cards with color coding, variant ranking with expandable content, and iteration trajectory chart"
    - "Simulation tab displays 8 MiroFish metrics, sentiment timeline chart, and clickable agent grid"
    - "Clicking an agent card opens the interview modal dialog for agent chat"
    - "Running campaigns show real-time progress via ProgressStream in the header area"
  artifacts:
    - path: "ui/src/pages/campaign-detail.tsx"
      provides: "Fully wired CampaignDetail page with all 3 tabs and progress display"
      contains: "ScoreCard"
      min_lines: 200
  key_links:
    - from: "ui/src/pages/campaign-detail.tsx"
      to: "ui/src/components/results/score-card.tsx"
      via: "import { ScoreCard }"
      pattern: "import.*ScoreCard.*from.*score-card"
    - from: "ui/src/pages/campaign-detail.tsx"
      to: "ui/src/components/results/variant-ranking.tsx"
      via: "import { VariantRanking }"
      pattern: "import.*VariantRanking.*from.*variant-ranking"
    - from: "ui/src/pages/campaign-detail.tsx"
      to: "ui/src/components/results/iteration-chart.tsx"
      via: "import { IterationChart }"
      pattern: "import.*IterationChart.*from.*iteration-chart"
    - from: "ui/src/pages/campaign-detail.tsx"
      to: "ui/src/components/simulation/metrics-panel.tsx"
      via: "import { MetricsPanel }"
      pattern: "import.*MetricsPanel.*from.*metrics-panel"
    - from: "ui/src/pages/campaign-detail.tsx"
      to: "ui/src/components/simulation/sentiment-timeline.tsx"
      via: "import { SentimentTimeline }"
      pattern: "import.*SentimentTimeline.*from.*sentiment-timeline"
    - from: "ui/src/pages/campaign-detail.tsx"
      to: "ui/src/components/simulation/agent-grid.tsx"
      via: "import { AgentGrid }"
      pattern: "import.*AgentGrid.*from.*agent-grid"
    - from: "ui/src/pages/campaign-detail.tsx"
      to: "ui/src/components/simulation/agent-interview.tsx"
      via: "import { AgentInterview }"
      pattern: "import.*AgentInterview.*from.*agent-interview"
    - from: "ui/src/pages/campaign-detail.tsx"
      to: "ui/src/components/progress/progress-stream.tsx"
      via: "import { ProgressStream }"
      pattern: "import.*ProgressStream.*from.*progress-stream"
---

<objective>
Wire all orphaned UI components into campaign-detail.tsx to close the parallel merge gap.

Purpose: Plans 08-05, 08-06, and 08-07 all ran in parallel and each produced their own version of campaign-detail.tsx. The final merge (08-07) overwrote Campaign and Simulation tab content with stubs. All 8 orphaned components are substantive and correct -- they just need to be imported and rendered in the right tab sections. This single-file rewrite unblocks requirements UI-05, UI-06, UI-07, UI-08, and UI-10.

Output: Fully wired campaign-detail.tsx with Campaign tab (ScoreCard, VariantRanking, IterationChart), Simulation tab (MetricsPanel, SentimentTimeline, AgentGrid, AgentInterview), and ProgressStream for running campaigns.
</objective>

<execution_context>
@C:/Users/adars/Downloads/ARC_Studio/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/adars/Downloads/ARC_Studio/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-ui-dashboard/08-05-SUMMARY.md
@.planning/phases/08-ui-dashboard/08-06-SUMMARY.md

<interfaces>
<!-- All components are already built. The executor needs their exact prop signatures to wire correctly. -->

From ui/src/components/results/score-card.tsx:
```typescript
interface ScoreCardProps {
  name: string;       // composite score key, e.g. "attention_score"
  value: number | null;
  description?: string;
}
export function ScoreCard({ name, value, description }: ScoreCardProps)
```

From ui/src/components/results/variant-ranking.tsx:
```typescript
interface VariantRankingProps {
  variants: IterationRecord[];  // from @/api/types
}
export function VariantRanking({ variants }: VariantRankingProps)
```

From ui/src/components/results/iteration-chart.tsx:
```typescript
interface IterationChartProps {
  iterations: IterationRecord[];  // from @/api/types
}
export function IterationChart({ iterations }: IterationChartProps)
```

From ui/src/components/simulation/metrics-panel.tsx:
```typescript
interface MetricsPanelProps {
  metrics: MirofishMetrics | null | undefined;  // from @/api/types
}
export function MetricsPanel({ metrics }: MetricsPanelProps)
```

From ui/src/components/simulation/sentiment-timeline.tsx:
```typescript
interface SentimentTimelineProps {
  trajectory: number[] | null | undefined;
}
export function SentimentTimeline({ trajectory }: SentimentTimelineProps)
```

From ui/src/components/simulation/agent-grid.tsx:
```typescript
export interface AgentData {
  id?: string; agent_id?: string; name?: string; role?: string;
  archetype?: string; persona?: string; sentiment?: number;
  influence_score?: number; posts_count?: number; platform?: string;
  [key: string]: unknown;
}
interface AgentGridProps {
  agents: AgentData[];
  onInterviewAgent: (agentId: string, agentName: string) => void;
}
export function AgentGrid({ agents, onInterviewAgent }: AgentGridProps)
```

From ui/src/components/simulation/agent-interview.tsx:
```typescript
interface AgentInterviewProps {
  campaignId: string;
  agentId: string;
  agentName: string;
  open: boolean;
  onOpenChange: (open: boolean) => void;
}
export function AgentInterview({ campaignId, agentId, agentName, open, onOpenChange }: AgentInterviewProps)
```

From ui/src/components/progress/progress-stream.tsx:
```typescript
interface ProgressStreamProps {
  campaignId: string;
}
export function ProgressStream({ campaignId }: ProgressStreamProps)
```

From ui/src/api/types.ts (data structures the executor must extract from):
```typescript
interface CampaignResponse {
  id: string;
  status: CampaignStatus;  // 'pending' | 'running' | 'completed' | 'failed'
  iterations?: IterationRecord[] | null;
  // ... other fields
}

interface IterationRecord {
  iteration_number: number;
  variant_id: string;
  variant_content: string;
  variant_strategy?: string | null;
  tribe_scores?: TribeScores | null;
  mirofish_metrics?: MirofishMetrics | null;
  composite_scores?: CompositeScores | null;
}

interface CompositeScores {
  attention_score: number | null;
  virality_potential: number | null;
  backlash_risk: number | null;
  memory_durability: number | null;
  conversion_potential: number | null;
  audience_fit: number | null;
  polarization_index: number | null;
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite campaign-detail.tsx to wire all orphaned components</name>
  <files>ui/src/pages/campaign-detail.tsx</files>
  <read_first>
    - ui/src/pages/campaign-detail.tsx (current stub version -- understand the existing Report tab and header structure to preserve)
    - ui/src/components/results/score-card.tsx (ScoreCardProps: name, value, description)
    - ui/src/components/results/variant-ranking.tsx (VariantRankingProps: variants: IterationRecord[])
    - ui/src/components/results/iteration-chart.tsx (IterationChartProps: iterations: IterationRecord[])
    - ui/src/components/simulation/metrics-panel.tsx (MetricsPanelProps: metrics: MirofishMetrics | null | undefined)
    - ui/src/components/simulation/sentiment-timeline.tsx (SentimentTimelineProps: trajectory: number[] | null | undefined)
    - ui/src/components/simulation/agent-grid.tsx (AgentGridProps: agents: AgentData[], onInterviewAgent: (id, name) => void)
    - ui/src/components/simulation/agent-interview.tsx (AgentInterviewProps: campaignId, agentId, agentName, open, onOpenChange)
    - ui/src/components/progress/progress-stream.tsx (ProgressStreamProps: campaignId: string)
    - ui/src/api/types.ts (CampaignResponse.iterations, IterationRecord.composite_scores, IterationRecord.mirofish_metrics)
  </read_first>
  <action>
    Rewrite ui/src/pages/campaign-detail.tsx. PRESERVE the existing ReportTabContent function, ReportSkeleton, the campaign header layout, and the Tabs structure. REPLACE the Campaign tab stub and Simulation tab stub with real component wiring. ADD ProgressStream and AgentInterview.

    **Specific changes:**

    1. ADD these imports at the top (alongside existing imports):
       - `import { useState } from 'react';`
       - `import { ScoreCard } from '@/components/results/score-card';`
       - `import { VariantRanking } from '@/components/results/variant-ranking';`
       - `import { IterationChart } from '@/components/results/iteration-chart';`
       - `import { MetricsPanel } from '@/components/simulation/metrics-panel';`
       - `import { SentimentTimeline } from '@/components/simulation/sentiment-timeline';`
       - `import { AgentGrid } from '@/components/simulation/agent-grid';`
       - `import { AgentInterview } from '@/components/simulation/agent-interview';`
       - `import { ProgressStream } from '@/components/progress/progress-stream';`
       - `import type { AgentData } from '@/components/simulation/agent-grid';`
       - `import type { CompositeScores } from '@/api/types';`

    2. ADD helper constants and extraction logic ABOVE the CampaignDetail component:

       Define `COMPOSITE_KEYS` array listing all 7 keys of CompositeScores:
       `['attention_score', 'virality_potential', 'conversion_potential', 'audience_fit', 'memory_durability', 'backlash_risk', 'polarization_index']`

       Define `SCORE_DESCRIPTIONS: Record<string, string>` mapping each composite key to a brief human-readable description:
       - attention_score: "How strongly the content captures and holds attention"
       - virality_potential: "Likelihood of organic sharing and spread"
       - conversion_potential: "Ability to drive desired action"
       - audience_fit: "Resonance with the target demographic"
       - memory_durability: "How well content sticks in long-term memory"
       - backlash_risk: "Risk of negative backlash (lower is better)"
       - polarization_index: "Degree of opinion polarization (lower is better)"

    3. ADD a `CampaignTabContent` function component accepting `{ campaign }`:
       - Extract `latestIterations` = all IterationRecord where iteration_number equals the max iteration_number in campaign.iterations (handle null/empty gracefully)
       - Extract `bestScores`: from latestIterations, find the variant with highest average non-null composite score, return its composite_scores (or null)
       - Extract `allIterations` = campaign.iterations ?? []
       - If no iterations, render an empty state: centered div with muted text "No iteration data available yet."
       - Otherwise render in a `<div className="space-y-8">`:
         - Section header "Composite Scores" as `<h3 className="text-sm font-medium text-foreground">Composite Scores</h3>`
         - Grid of 7 ScoreCard components: `<div className="grid grid-cols-2 gap-3 lg:grid-cols-4">` mapping COMPOSITE_KEYS to `<ScoreCard key={key} name={key} value={bestScores?.[key] ?? null} description={SCORE_DESCRIPTIONS[key]} />`
         - Section header "Variant Ranking"
         - `<VariantRanking variants={latestIterations} />`
         - Section header "Score Trajectory"
         - `<IterationChart iterations={allIterations} />`

    4. ADD a `SimulationTabContent` function component accepting `{ campaign }`:
       - Extract `latestIteration` = the first IterationRecord from campaign.iterations with the max iteration_number (single record, not array) -- handle null gracefully
       - Extract `metrics` = latestIteration?.mirofish_metrics ?? null
       - Extract `trajectory` = metrics?.sentiment_trajectory ?? null
       - Extract agents: attempt to pull agent data from the campaign. Since agent_stats is not in the API response (known limitation from 08-06 SUMMARY), pass an empty array `[] as AgentData[]`. The AgentGrid component already handles this with a "not available" empty state.
       - If campaign.iterations is null or empty, render centered empty state "No simulation data available yet."
       - Otherwise render in a `<div className="space-y-8">`:
         - `<MetricsPanel metrics={metrics} />`
         - `<SentimentTimeline trajectory={trajectory} />`
         - `<AgentGrid agents={agents} onInterviewAgent={handleInterviewAgent} />`
       - The `handleInterviewAgent` callback sets the `interviewAgent` state (see step 5)

    5. ADD state and handler for agent interview in the main CampaignDetail component:
       - `const [interviewAgent, setInterviewAgent] = useState<{ id: string; name: string } | null>(null);`
       - `const handleInterviewAgent = (agentId: string, agentName: string) => setInterviewAgent({ id: agentId, name: agentName });`
       - Pass `handleInterviewAgent` down to SimulationTabContent via prop

    6. ADD ProgressStream conditional render in the campaign header area:
       - Between the header metadata div and the Tabs, add:
         `{campaign.status === 'running' && <ProgressStream campaignId={id!} />}`

    7. REPLACE Campaign tab TabsContent with:
       `<CampaignTabContent campaign={campaign} />`

    8. REPLACE Simulation tab TabsContent with:
       `<SimulationTabContent campaign={campaign} onInterviewAgent={handleInterviewAgent} />`

    9. ADD AgentInterview dialog AFTER the closing Tabs tag (inside the main return div, after Tabs):
       ```
       <AgentInterview
         campaignId={campaign.id}
         agentId={interviewAgent?.id ?? ''}
         agentName={interviewAgent?.name ?? ''}
         open={interviewAgent !== null}
         onOpenChange={(open) => { if (!open) setInterviewAgent(null); }}
       />
       ```

    10. CHANGE the Tabs defaultValue from "report" to "campaign" since Campaign tab now has real content.

    11. UPDATE the JSDoc comment at the top to remove the line "Campaign and Simulation tab content are placeholders until plans 08-05 and 08-06 wire their respective components." Replace with "All three tabs fully wired: Campaign (score cards, variant ranking, iteration chart), Simulation (metrics, sentiment, agent grid, interview modal), Report (4 layers + export)."

    **DO NOT modify any other file.** All components are already built and correct. This is purely a wiring task in campaign-detail.tsx.
  </action>
  <verify>
    <automated>cd C:/Users/adars/Downloads/ARC_Studio/ui && npx tsc --noEmit 2>&1 | head -30</automated>
  </verify>
  <acceptance_criteria>
    - grep -c "import.*ScoreCard.*from.*score-card" ui/src/pages/campaign-detail.tsx returns 1
    - grep -c "import.*VariantRanking.*from.*variant-ranking" ui/src/pages/campaign-detail.tsx returns 1
    - grep -c "import.*IterationChart.*from.*iteration-chart" ui/src/pages/campaign-detail.tsx returns 1
    - grep -c "import.*MetricsPanel.*from.*metrics-panel" ui/src/pages/campaign-detail.tsx returns 1
    - grep -c "import.*SentimentTimeline.*from.*sentiment-timeline" ui/src/pages/campaign-detail.tsx returns 1
    - grep -c "import.*AgentGrid.*from.*agent-grid" ui/src/pages/campaign-detail.tsx returns 1
    - grep -c "import.*AgentInterview.*from.*agent-interview" ui/src/pages/campaign-detail.tsx returns 1
    - grep -c "import.*ProgressStream.*from.*progress-stream" ui/src/pages/campaign-detail.tsx returns 1
    - grep -c "interviewAgent" ui/src/pages/campaign-detail.tsx returns at least 3 (state, setter, dialog)
    - grep -c "status.*running.*ProgressStream" ui/src/pages/campaign-detail.tsx returns 1
    - npx tsc --noEmit exits with code 0 (no type errors)
    - npm run build exits with code 0 (production build succeeds)
  </acceptance_criteria>
  <done>
    campaign-detail.tsx imports and renders all 8 previously-orphaned components. Campaign tab shows 7 score cards + variant ranking + iteration chart from campaign.iterations data. Simulation tab shows MiroFish metrics panel + sentiment timeline + agent grid (empty state for agents is acceptable per known limitation). Agent interview modal opens when agent card is clicked. ProgressStream renders for running campaigns. TypeScript compiles with zero errors. Production build succeeds.
  </done>
</task>

<task type="auto">
  <name>Task 2: Verify build and all component wiring</name>
  <files>ui/src/pages/campaign-detail.tsx</files>
  <read_first>
    - ui/src/pages/campaign-detail.tsx (after Task 1 rewrite)
  </read_first>
  <action>
    Run the full verification suite to confirm the gap closure is complete:

    1. Run `cd ui && npx tsc --noEmit` -- must exit 0 with no type errors
    2. Run `cd ui && npm run build` -- must exit 0 with no build errors
    3. Verify all 8 imports exist in campaign-detail.tsx using grep
    4. Verify no placeholder stub text remains: grep for "Campaign overview content renders here" and "Simulation results render here" -- both must return 0 matches
    5. Verify interviewAgent state exists: grep for "useState.*interviewAgent" -- must return 1 match
    6. Verify ProgressStream conditional: grep for "status.*running" -- must return at least 1 match

    If any check fails, fix the issue in campaign-detail.tsx and re-run.
  </action>
  <verify>
    <automated>cd C:/Users/adars/Downloads/ARC_Studio/ui && npm run build 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - npm run build exits 0
    - grep "Campaign overview content renders here" ui/src/pages/campaign-detail.tsx returns 0 matches
    - grep "Simulation results render here" ui/src/pages/campaign-detail.tsx returns 0 matches
  </acceptance_criteria>
  <done>
    Production build passes. No stub placeholders remain. All 8 orphaned components are wired. Gap closure verified.
  </done>
</task>

</tasks>

<verification>
1. `cd ui && npx tsc --noEmit` -- zero type errors
2. `cd ui && npm run build` -- zero build errors
3. All 8 component imports present in campaign-detail.tsx
4. No placeholder stub text ("Campaign overview content renders here" / "Simulation results render here") remains
5. interviewAgent state management wired (useState + onOpenChange + AgentInterview dialog)
6. ProgressStream conditionally rendered for running campaigns
</verification>

<success_criteria>
- UI-05: CampaignDetail has 3 functional tabs (Campaign, Simulation, Report) -- all rendering real components
- UI-06: Campaign tab shows ScoreCard grid (7 scores), VariantRanking, and IterationChart from campaign.iterations
- UI-07: Simulation tab shows MetricsPanel (8 metrics), SentimentTimeline, and AgentGrid
- UI-08: AgentInterview modal dialog is rendered and wired to AgentGrid's onInterviewAgent callback
- UI-10: ProgressStream rendered when campaign.status === 'running'
- TypeScript compiles with 0 errors
- Production build succeeds with 0 errors
</success_criteria>

<output>
After completion, create `.planning/phases/08-ui-dashboard/08-08-SUMMARY.md`
</output>

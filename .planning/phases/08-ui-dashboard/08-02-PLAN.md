---
phase: 08-ui-dashboard
plan: 02
type: execute
wave: 2
depends_on: ["08-01"]
files_modified:
  - ui/src/App.tsx
  - ui/src/components/layout/app-layout.tsx
  - ui/src/components/layout/sidebar.tsx
  - ui/src/components/layout/header.tsx
  - ui/src/components/common/status-badge.tsx
  - ui/src/components/common/empty-state.tsx
  - ui/src/components/common/loading-skeleton.tsx
  - ui/src/components/common/error-state.tsx
  - ui/src/pages/campaign-list.tsx
autonomous: true
requirements:
  - UI-03
  - UI-11
  - UI-13

must_haves:
  truths:
    - "App renders with a fixed left sidebar and header"
    - "Sidebar shows campaign history list from API"
    - "Header displays app name and system health indicator"
    - "CampaignList page renders campaigns with status badges"
    - "Loading, error, and empty states display correctly"
    - "Clicking a campaign navigates to its detail route"
    - "Status badges show correct colors for pending/running/completed/failed"
  artifacts:
    - path: "ui/src/components/layout/app-layout.tsx"
      provides: "Main layout shell with sidebar + header + content area"
      min_lines: 30
    - path: "ui/src/components/layout/sidebar.tsx"
      provides: "Fixed left sidebar with campaign history"
      min_lines: 40
    - path: "ui/src/pages/campaign-list.tsx"
      provides: "Campaign list page with status badges and click-to-navigate"
      min_lines: 40
    - path: "ui/src/components/common/status-badge.tsx"
      provides: "Status badge component for campaign states"
      exports: ["StatusBadge"]
  key_links:
    - from: "ui/src/components/layout/sidebar.tsx"
      to: "ui/src/hooks/use-campaigns.ts"
      via: "useCampaigns() hook"
      pattern: "useCampaigns"
    - from: "ui/src/pages/campaign-list.tsx"
      to: "ui/src/components/common/status-badge.tsx"
      via: "StatusBadge import"
      pattern: "StatusBadge"
    - from: "ui/src/App.tsx"
      to: "ui/src/components/layout/app-layout.tsx"
      via: "Route wrapper layout"
      pattern: "AppLayout"
---

<objective>
Build the application shell (fixed sidebar + header layout per D-03), routing structure, CampaignList page with status badges, and all reusable common state components (loading, error, empty).

Purpose: The layout and navigation shell is the frame that all other pages live inside. CampaignList is the default landing page and proves the data flow from API through React Query to rendered UI.
Output: Working dashboard shell with sidebar, campaign list, and polished state handling.
</objective>

<execution_context>
@C:/Users/adars/Downloads/ARC_Studio/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/adars/Downloads/ARC_Studio/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/08-ui-dashboard/08-CONTEXT.md
@.planning/phases/08-ui-dashboard/08-RESEARCH.md
@.planning/phases/08-ui-dashboard/08-01-SUMMARY.md

<interfaces>
<!-- From Plan 01 outputs (types, hooks, API) -->
From ui/src/api/types.ts:
```typescript
export interface CampaignResponse {
  id: string;
  status: 'pending' | 'running' | 'completed' | 'failed';
  seed_content: string;
  prediction_question: string;
  demographic: string;
  created_at: string;
  started_at?: string | null;
  completed_at?: string | null;
  error?: string | null;
  // ... other fields
}
export interface CampaignListResponse { campaigns: CampaignResponse[]; total: number; }
export interface HealthResponse {
  orchestrator: string;
  tribe_scorer: ServiceHealth;
  mirofish: ServiceHealth;
  database: ServiceHealth;
}
export type CampaignStatus = 'pending' | 'running' | 'completed' | 'failed';
```

From ui/src/hooks/use-campaigns.ts:
```typescript
export function useCampaigns(): UseQueryResult<CampaignListResponse>;
export function useCampaign(id: string): UseQueryResult<CampaignResponse>;
```

From ui/src/hooks/use-health.ts:
```typescript
export function useHealth(): UseQueryResult<HealthResponse>;
```

From ui/src/utils/formatters.ts:
```typescript
export function formatDate(isoString: string): string;
export function formatMetricLabel(snakeCase: string): string;
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build layout shell (sidebar, header, routing) and common state components</name>
  <files>
    ui/src/App.tsx, ui/src/components/layout/app-layout.tsx,
    ui/src/components/layout/sidebar.tsx, ui/src/components/layout/header.tsx,
    ui/src/components/common/status-badge.tsx, ui/src/components/common/empty-state.tsx,
    ui/src/components/common/loading-skeleton.tsx, ui/src/components/common/error-state.tsx
  </files>
  <read_first>
    - ui/src/api/types.ts (CampaignResponse, HealthResponse, CampaignStatus for type imports)
    - ui/src/hooks/use-campaigns.ts (useCampaigns hook for sidebar)
    - ui/src/hooks/use-health.ts (useHealth hook for header)
    - ui/src/components/ui/badge.tsx (shadcn Badge component for status badges)
    - ui/src/components/ui/card.tsx (shadcn Card for list items)
    - ui/src/components/ui/skeleton.tsx (shadcn Skeleton for loading states)
    - ui/src/components/ui/scroll-area.tsx (shadcn ScrollArea for sidebar)
    - .planning/phases/08-ui-dashboard/08-CONTEXT.md (D-03 fixed sidebar, D-07 premium quality)
  </read_first>
  <action>
    Per D-03: Fixed left sidebar with campaign history list + main content area with header.
    Per D-07: CRITICAL -- premium, distinctive design. NOT a generic dashboard template.

    **ui/src/components/layout/app-layout.tsx:**
    - Fixed left sidebar (w-72 on desktop, collapsible on mobile) + main content area
    - Sidebar has a subtle border-right and slightly different bg tone from the main area
    - Main area has a header bar at top + scrollable content below
    - Use CSS grid or flex layout: `grid grid-cols-[18rem_1fr]` for the two-column layout
    - Children rendered in the main content area via Outlet or children prop
    - Add subtle transitions for polish per D-07

    **ui/src/components/layout/sidebar.tsx:**
    - Fixed left panel with app branding at top (Nexus Sim logo area -- use text-based logo with a distinctive mark)
    - "New Campaign" button (prominent, uses primary color) -- links to /campaigns/new
    - Campaign history list below using useCampaigns() hook, wrapped in shadcn ScrollArea
    - Each campaign entry shows: truncated prediction_question (first 60 chars), status badge, relative date
    - Active campaign highlighted (match current route param)
    - Clicking a campaign navigates to /campaigns/:id
    - Loading state shows 3-4 skeleton items
    - Empty state shows "No campaigns yet" message
    - Use lucide-react icons: Plus for new campaign, Activity or Zap for logo accent

    **ui/src/components/layout/header.tsx:**
    - Horizontal bar at top of main content area
    - Shows breadcrumb or page title (passed as prop or from route)
    - Right side: system health indicator dot -- green if all services ok, amber if partial, red if any down
    - Use useHealth() hook for status, poll every 60s
    - Health dot uses a small pulsing circle animation per D-07 premium feel

    **ui/src/components/common/status-badge.tsx:**
    - Accepts status: CampaignStatus prop
    - Returns shadcn Badge with appropriate variant and color:
      - pending: muted/gray, clock icon
      - running: blue/indigo with pulse animation, play icon
      - completed: green/emerald, check icon
      - failed: red, x-circle icon
    - Use lucide-react icons (Clock, Play, CheckCircle2, XCircle)

    **ui/src/components/common/loading-skeleton.tsx:**
    - Reusable skeleton layout for different contexts (campaign list, detail page, etc.)
    - Export CampaignListSkeleton (3-4 card skeletons) and CampaignDetailSkeleton

    **ui/src/components/common/error-state.tsx:**
    - Error display with icon (AlertTriangle from lucide), error message, and retry button
    - Accept props: message (string), onRetry (function)

    **ui/src/components/common/empty-state.tsx:**
    - Empty state with icon, title, description, and optional action button
    - Accept props: icon (LucideIcon), title, description, action? (label + onClick)

    **ui/src/App.tsx** -- Update with full routing:
    - "/" redirects to "/campaigns"
    - "/campaigns" renders CampaignList inside AppLayout
    - "/campaigns/new" renders placeholder inside AppLayout (Plan 03)
    - "/campaigns/:id" renders placeholder inside AppLayout (Plan 05)
    - Use React Router Outlet pattern with AppLayout as the layout route
    - All routes wrapped in AppLayout

    DESIGN QUALITY per D-07:
    - Sidebar should have subtle glassmorphism or layered depth (not flat gray box)
    - Use font-weight variations: semibold for headings, normal for body, light for secondary text
    - Status badges should feel crisp with appropriate icon + text
    - Campaign list items should have hover state with subtle bg change
    - Use gap and padding consistently (p-4, p-6 for sections, gap-2, gap-3 between elements)
  </action>
  <verify>
    <automated>cd C:/Users/adars/Downloads/ARC_Studio/ui && npm run build 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - ui/src/components/layout/app-layout.tsx renders a two-column layout with fixed sidebar
    - ui/src/components/layout/sidebar.tsx imports useCampaigns and renders campaign list
    - ui/src/components/layout/header.tsx imports useHealth and renders health indicator
    - ui/src/components/common/status-badge.tsx exports StatusBadge accepting status prop
    - ui/src/components/common/empty-state.tsx exports EmptyState
    - ui/src/components/common/loading-skeleton.tsx exports CampaignListSkeleton
    - ui/src/components/common/error-state.tsx exports ErrorState with onRetry prop
    - ui/src/App.tsx has routes for /campaigns, /campaigns/new, /campaigns/:id
    - npm run build succeeds
  </acceptance_criteria>
  <done>Layout shell renders with sidebar + header, routing works for 3 routes, common components available</done>
</task>

<task type="auto">
  <name>Task 2: Build CampaignList page with status badges, filtering, and polished data display</name>
  <files>
    ui/src/pages/campaign-list.tsx
  </files>
  <read_first>
    - ui/src/hooks/use-campaigns.ts (useCampaigns hook)
    - ui/src/components/common/status-badge.tsx (StatusBadge for status display)
    - ui/src/components/common/loading-skeleton.tsx (CampaignListSkeleton)
    - ui/src/components/common/empty-state.tsx (EmptyState)
    - ui/src/components/common/error-state.tsx (ErrorState)
    - ui/src/components/ui/card.tsx (shadcn Card)
    - ui/src/utils/formatters.ts (formatDate)
  </read_first>
  <action>
    Build the CampaignList page (UI-11) -- the main landing page of the dashboard.

    **ui/src/pages/campaign-list.tsx:**
    - Page title "Campaigns" rendered in the content area
    - Uses useCampaigns() hook for data
    - Three states handled per UI-13:
      1. Loading: CampaignListSkeleton
      2. Error: ErrorState with retry (refetch from React Query)
      3. Empty: EmptyState with "Create your first campaign" CTA button linking to /campaigns/new
    - Campaign cards in a grid layout (1 col on mobile, 2 on md, 3 on lg)
    - Each card (shadcn Card) shows:
      - Status badge (top-right corner of card)
      - Prediction question (truncated to 2 lines with line-clamp)
      - Demographic label
      - Created date (relative via formatDate)
      - Iteration count (if completed: "3/4 iterations")
      - Card is clickable (entire card is a Link to /campaigns/:id)
      - Hover state: subtle border-color or bg shift
    - Cards should be visually distinct per status: running cards could have a subtle animated border gradient per D-07

    DESIGN per D-07:
    - Cards should have consistent spacing and feel data-rich but not cluttered
    - The grid should breathe -- adequate gap between cards (gap-4 or gap-6)
    - Use the secondary bg color for cards against the main background for depth
    - Status badge placement should be consistent and instantly scannable
  </action>
  <verify>
    <automated>cd C:/Users/adars/Downloads/ARC_Studio/ui && npm run build 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - ui/src/pages/campaign-list.tsx imports useCampaigns, StatusBadge, CampaignListSkeleton, EmptyState, ErrorState
    - Campaign cards render in a responsive grid
    - Loading state shows skeleton
    - Error state shows error with retry
    - Empty state shows CTA to create campaign
    - Each card is a link to /campaigns/:id
    - npm run build succeeds
  </acceptance_criteria>
  <done>CampaignList page renders all campaigns with status badges, handles loading/error/empty states, and navigates to detail on click</done>
</task>

</tasks>

<verification>
- `cd ui && npm run build` succeeds
- App renders with sidebar + header layout when visited at localhost:5173
- /campaigns route shows campaign list (or empty state)
- /campaigns/new route renders placeholder
- Sidebar shows campaign history
- Health indicator visible in header
- Status badges show correct styling for each status
</verification>

<success_criteria>
- Fixed sidebar layout with campaign history list renders correctly
- Header shows system health indicator
- CampaignList page displays campaigns in a grid with status badges
- Loading, error, and empty states all render appropriately
- Navigation between routes works (sidebar links, card clicks)
- Design feels premium and distinctive per D-07 (not a template)
</success_criteria>

<output>
After completion, create `.planning/phases/08-ui-dashboard/08-02-SUMMARY.md`
</output>

---
phase: 08-ui-dashboard
plan: 07
type: execute
wave: 3
depends_on: ["08-05"]
files_modified:
  - ui/src/components/results/verdict-display.tsx
  - ui/src/components/results/scorecard-table.tsx
  - ui/src/components/results/deep-analysis.tsx
  - ui/src/components/results/mass-psychology.tsx
  - ui/src/components/results/export-buttons.tsx
  - ui/src/hooks/use-report.ts
  - ui/src/pages/campaign-detail.tsx
autonomous: true
requirements:
  - UI-09
  - UI-12

must_haves:
  truths:
    - "Report tab shows Layer 1 verdict as plain English text"
    - "Report tab shows Layer 2 scorecard with variant ranking table"
    - "Report tab has expandable Layer 3 deep analysis sections"
    - "Report tab has Layer 4 mass psychology with general/technical toggle"
    - "JSON export button triggers file download"
    - "Markdown export button triggers file download"
  artifacts:
    - path: "ui/src/components/results/verdict-display.tsx"
      provides: "Layer 1 verdict display"
      exports: ["VerdictDisplay"]
    - path: "ui/src/components/results/scorecard-table.tsx"
      provides: "Layer 2 scorecard with variant ranking"
      exports: ["ScorecardTable"]
    - path: "ui/src/components/results/deep-analysis.tsx"
      provides: "Layer 3 expandable analysis sections"
      exports: ["DeepAnalysis"]
    - path: "ui/src/components/results/mass-psychology.tsx"
      provides: "Layer 4 with general/technical toggle"
      exports: ["MassPsychology"]
    - path: "ui/src/components/results/export-buttons.tsx"
      provides: "JSON + Markdown export download triggers"
      exports: ["ExportButtons"]
  key_links:
    - from: "ui/src/hooks/use-report.ts"
      to: "ui/src/api/reports.ts"
      via: "getReport API call"
      pattern: "getReport"
    - from: "ui/src/components/results/export-buttons.tsx"
      to: "ui/src/api/reports.ts"
      via: "downloadExport function"
      pattern: "downloadExport"
    - from: "ui/src/pages/campaign-detail.tsx"
      to: "ui/src/components/results/verdict-display.tsx"
      via: "Report tab content"
      pattern: "VerdictDisplay"
---

<objective>
Build the Report tab content with all 4 report layers (verdict, scorecard, deep analysis, mass psychology) and export functionality (JSON + Markdown download buttons).

Purpose: The Report tab presents the culmination of the analysis -- the actionable insights. Each layer serves a different audience: verdict for executives, scorecard for analysts, deep analysis for data scientists, mass psychology for researchers.
Output: Complete Report tab with all 4 layers rendering from API data, plus working export buttons.
</objective>

<execution_context>
@C:/Users/adars/Downloads/ARC_Studio/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/adars/Downloads/ARC_Studio/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/08-ui-dashboard/08-CONTEXT.md
@.planning/phases/08-ui-dashboard/08-RESEARCH.md
@.planning/phases/08-ui-dashboard/08-01-SUMMARY.md
@.planning/phases/08-ui-dashboard/08-05-SUMMARY.md

<interfaces>
From ui/src/api/types.ts:
```typescript
export interface ReportResponse {
  id: string;
  campaign_id: string;
  verdict?: string | null;
  scorecard?: ScorecardData | null;
  deep_analysis?: Record<string, any> | null;
  mass_psychology_general?: string | null;
  mass_psychology_technical?: string | null;
  created_at: string;
}

export interface ScorecardData {
  winning_variant_id: string;
  variants: ScorecardVariant[];
  iteration_trajectory: Record<string, any>[];
  thresholds_status: Record<string, any>;
  summary: string;
}

export interface ScorecardVariant {
  variant_id: string;
  rank: number;
  strategy: string;
  composite_scores: Record<string, number | null>;
  color_coding: Record<string, string>;  // Pre-computed by backend per Pitfall 8
}
```

From ui/src/api/reports.ts:
```typescript
export function getReport(campaignId: string): Promise<ReportResponse>;
export function downloadExport(campaignId: string, format: 'json' | 'markdown'): void;
```

From orchestrator/api/reports.py:
- GET /api/campaigns/{id}/report -> ReportResponse
- GET /api/campaigns/{id}/export/json -> file download
- GET /api/campaigns/{id}/export/markdown -> file download
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build report hook and all 4 report layer components</name>
  <files>
    ui/src/hooks/use-report.ts,
    ui/src/components/results/verdict-display.tsx,
    ui/src/components/results/scorecard-table.tsx,
    ui/src/components/results/deep-analysis.tsx,
    ui/src/components/results/mass-psychology.tsx
  </files>
  <read_first>
    - ui/src/api/types.ts (ReportResponse, ScorecardData, ScorecardVariant)
    - ui/src/api/reports.ts (getReport function)
    - ui/src/components/ui/collapsible.tsx (shadcn Collapsible for expandable sections)
    - ui/src/components/ui/toggle-group.tsx (for general/technical toggle)
    - ui/src/components/ui/card.tsx (shadcn Card)
    - ui/src/utils/colors.ts (for score color coding if needed beyond backend-provided)
    - ui/src/utils/formatters.ts (formatScore, formatMetricLabel)
    - docs/Results.md (output layer specifications -- read section 3.1 for display requirements)
    - .planning/phases/08-ui-dashboard/08-RESEARCH.md (Pitfall 8: use color_coding from backend for scorecard)
  </read_first>
  <action>
    **ui/src/hooks/use-report.ts:**
    ```typescript
    export function useReport(campaignId: string) {
      return useQuery({
        queryKey: ['report', campaignId],
        queryFn: () => getReport(campaignId),
        retry: 1,
        // Only fetch report for completed campaigns
        enabled: !!campaignId,
      });
    }
    ```

    **ui/src/components/results/verdict-display.tsx:**
    Props: { verdict: string | null }
    - Layer 1: Plain English recommendation (100-400 words, no jargon per RPT-01)
    - Render as a prominent text block at the top of the Report tab
    - Use a slightly distinct background or left border accent to set it apart
    - Render markdown-like formatting if present (paragraphs, bold, etc.)
    - If verdict is null, show "Report not yet generated" with muted styling
    - DESIGN per D-07: The verdict should feel like reading an executive brief -- elegant typography, generous line height, comfortable reading width (max-w-prose or similar)

    **ui/src/components/results/scorecard-table.tsx:**
    Props: { scorecard: ScorecardData | null }
    - Layer 2: Structured ranking data (RPT-02)
    - Section: "Winner: {winning_variant_id}" badge/callout
    - Variant ranking table:
      - Columns: Rank, Variant ID, Strategy, then one column per composite score
      - Each score cell colored using color_coding from the backend (Per Pitfall 8 -- do NOT recompute colors)
      - Color mapping: "green" -> emerald bg, "amber" -> amber bg, "red" -> red bg
    - Section: Iteration trajectory (summary from scorecard data)
    - Section: Threshold status -- which thresholds are met (check/x indicators)
    - Section: Scorecard summary text at bottom
    - Use shadcn table-like styling (or Card-based layout for a more modern feel)
    - Handle null scorecard with "Scorecard not available" message

    **ui/src/components/results/deep-analysis.tsx:**
    Props: { deepAnalysis: Record<string, any> | null }
    - Layer 3: Per-iteration detailed data (RPT-03)
    - Expandable sections using shadcn Collapsible -- one per iteration
    - Each iteration section shows:
      - Iteration number as section header
      - Per-variant data: TRIBE scores table, MiroFish metrics table, composite scores table
      - Analysis insights (ranking, cross_system_insights list)
    - All sections collapsed by default (deep analysis is for power users)
    - Expand all / Collapse all toggle at the top
    - If deep_analysis is null, show "Deep analysis not available"
    - Data structure from backend: { iterations: [{ iteration: 1, variants: [...], analysis: {...} }] }

    **ui/src/components/results/mass-psychology.tsx:**
    Props: { general: string | null, technical: string | null }
    - Layer 4: Mass psychology narrative (RPT-04, RPT-05)
    - Toggle between "General" and "Technical" views using shadcn ToggleGroup or Tabs
    - General: Narrative prose about crowd dynamics (200-600 words)
    - Technical: Psychology theory references with named theories
    - Both rendered as rich text blocks with comfortable reading typography
    - Default to "General" view
    - If neither is available, show "Psychology analysis not available"
    - DESIGN per D-07: The psychology sections should feel scholarly -- slightly different typographic treatment, perhaps a serif font or subtle blockquote styling for the technical section to convey academic weight
  </action>
  <verify>
    <automated>cd C:/Users/adars/Downloads/ARC_Studio/ui && npm run build 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - ui/src/hooks/use-report.ts exports useReport hook using useQuery
    - ui/src/components/results/verdict-display.tsx exports VerdictDisplay, handles null verdict
    - ui/src/components/results/scorecard-table.tsx exports ScorecardTable, uses color_coding from backend (NOT recomputing)
    - ui/src/components/results/deep-analysis.tsx exports DeepAnalysis using shadcn Collapsible, sections collapsed by default
    - ui/src/components/results/mass-psychology.tsx exports MassPsychology with general/technical toggle
    - All components handle null data gracefully
    - npm run build succeeds
  </acceptance_criteria>
  <done>All 4 report layers render correctly: verdict as rich text, scorecard as ranked table with backend colors, deep analysis as expandable sections, mass psychology with general/technical toggle</done>
</task>

<task type="auto">
  <name>Task 2: Build export buttons and wire Report tab into CampaignDetail page</name>
  <files>
    ui/src/components/results/export-buttons.tsx,
    ui/src/pages/campaign-detail.tsx
  </files>
  <read_first>
    - ui/src/api/reports.ts (downloadExport function using window.open)
    - ui/src/hooks/use-report.ts (useReport hook)
    - ui/src/pages/campaign-detail.tsx (current state -- need to wire Report tab)
    - ui/src/components/ui/button.tsx (shadcn Button)
    - .planning/phases/08-ui-dashboard/08-RESEARCH.md (Pitfall 7: file download via window.open)
  </read_first>
  <action>
    **ui/src/components/results/export-buttons.tsx:**
    Props: { campaignId: string }
    - Two buttons: "Export JSON" and "Export Markdown"
    - JSON button: onClick calls downloadExport(campaignId, 'json')
    - Markdown button: onClick calls downloadExport(campaignId, 'markdown')
    - Per Pitfall 7: downloadExport uses window.open() to trigger browser download (no auth needed in POC)
    - Buttons styled as secondary/outline variant with Download icon from lucide-react
    - Grouped horizontally with small gap

    **Wire Report tab in ui/src/pages/campaign-detail.tsx:**
    - Import useReport hook, all report components, and ExportButtons
    - In Report tab content area:
      - If campaign is not completed, show "Campaign must complete before report is available"
      - If completed, fetch report via useReport(campaignId)
      - Report tab layout (top to bottom):
        1. ExportButtons (top-right, above content)
        2. VerdictDisplay (Layer 1)
        3. ScorecardTable (Layer 2)
        4. DeepAnalysis (Layer 3, collapsed)
        5. MassPsychology (Layer 4)
      - Each section separated by dividers or spacing
      - Loading state for report: skeleton
      - Error state: error message with retry
    - Handle the case where report is not yet generated (ReportResponse could be 404)
  </action>
  <verify>
    <automated>cd C:/Users/adars/Downloads/ARC_Studio/ui && npm run build 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - ui/src/components/results/export-buttons.tsx exports ExportButtons with JSON and Markdown buttons
    - ExportButtons calls downloadExport (window.open) on click
    - ui/src/pages/campaign-detail.tsx Report tab imports and renders VerdictDisplay, ScorecardTable, DeepAnalysis, MassPsychology, ExportButtons
    - Report tab fetches report data via useReport hook
    - Report tab shows appropriate message for non-completed campaigns
    - npm run build succeeds
  </acceptance_criteria>
  <done>Report tab renders all 4 layers with export buttons, handles loading/error/no-report states. Export triggers browser downloads via window.open.</done>
</task>

</tasks>

<verification>
- `cd ui && npm run build` succeeds
- Report tab renders all 4 layers
- Scorecard uses backend color_coding per Pitfall 8
- Deep analysis sections are expandable/collapsible
- Mass psychology has general/technical toggle
- Export buttons trigger downloads
</verification>

<success_criteria>
- Report tab renders all 4 report layers from API data
- Layer 1 verdict displays as prominent readable text
- Layer 2 scorecard shows ranked table with backend-provided colors
- Layer 3 deep analysis uses collapsible sections (collapsed by default)
- Layer 4 mass psychology has general/technical toggle
- JSON and Markdown export buttons trigger file downloads
- All sections handle null/missing data gracefully
- Premium typography and layout per D-07
</success_criteria>

<output>
After completion, create `.planning/phases/08-ui-dashboard/08-07-SUMMARY.md`
</output>
